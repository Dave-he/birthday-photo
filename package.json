{
  "name": "birthday-photo-monorepo",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:unit": "node --test --experimental-strip-types --no-warnings tests/unit/*.test.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
